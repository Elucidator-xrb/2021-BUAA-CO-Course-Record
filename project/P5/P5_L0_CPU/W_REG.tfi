


// Instantiate the module
W_REG instance_name (
    .Clk(Clk), 
    .Rst(Rst), 
    .We(We), 
    .IR_in(IR_in), 
    .PC_in(PC_in), 
    .DR_in(DR_in), 
    .Y_in(Y_in), 
    .IR_out(IR_out), 
    .PC_out(PC_out), 
    .DR_out(DR_out), 
    .Y_out(Y_out)
    );


